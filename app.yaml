runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  python_version: 3

env_variables:
  PROJECT: imagelabeldetector-293221
  GCS_IMAGE_BUCKET: imagelabeldetector-293221-images
